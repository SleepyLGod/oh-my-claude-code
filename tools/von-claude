#!/bin/sh
set -eu

REPO_ROOT="${VON_CLAUDE_REPO_ROOT:-/Users/von/Projects/claude-code}"

export CLAUDE_CONFIG_DIR="${CLAUDE_CONFIG_DIR:-$HOME/.von-claude}"

cd "$REPO_ROOT"
exec bun run ./src/bootstrap-entry.ts "$@"
